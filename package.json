{
  "private": true,
  "name": "coffee-o-meter",
  "version": "1.0.0",
  "workspaces": [
    "docs",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "dotenv -e .env.local -- turbo run dev"
  },
  "dependencies": {
    "dotenv-cli": "^7.0.0"
  }
}
